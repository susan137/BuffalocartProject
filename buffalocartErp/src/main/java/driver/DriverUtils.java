package driver;

import org.openqa.selenium.WebDriver;

public class DriverUtils {

public static WebDriver maximize(WebDriver driver) {
		
		driver.manage().window().maximize();  
		return driver;
	}
	
public static WebDriver waitTime(WebDriver driver) {
		
		driver.manage().window().maximize();  
		return driver;
	}
	

}
